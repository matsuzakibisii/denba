# GA4 日次レポート 2026-08-31（月）

> 2026-09-01 実行分（処理が日付をまたいだため一部作業は 09-02 実施）。
> 未取得だった **08-30**、および **09-01** も遡って取得・追記した。
> アカウント違いによる取得不能は解消し、GA4プロパティ「Denba」（`a400400023p544645937`）へ正常アクセスできた。

## 主要指標

| 指標 | 08-30 | 08-31 | 09-01 | 前日比 |
|---|---|---|---|---|
| アクティブユーザー | 0 | 0 | 0 | ±0 |
| 新規ユーザー | 0 | 0 | 0 | ±0 |
| セッション | 0 | 0 | 0 | ±0 |
| キーイベント | 0 | 0 | 0 | ±0 |
| 平均エンゲージメント時間 | 0秒 | 0秒 | 0秒 | ±0 |

GA4トラフィック獲得レポートは3日とも「データがありません」。全チャネル（Organic Search / Referral / AI Assistant / Direct / Unassigned）0セッション。上位ページも該当なし。

**これで 8/20〜9/1 の13日連続ゼロが確定した。**

---

## 【重要】GA4計測の健全性チェック — 結果：**計測は正常**

8/29・W35週次・8月月次の3レポートで「最優先」として繰り越されていた「実訪問ゼロなのか、タグが壊れているのか」の切り分けを、今回実施して**決着させた**。

### 実施した検証と結果

| 検証 | 結果 | 判定 |
|---|---|---|
| 全HTMLのタグ有無（`grep -L "G-CCCN9E7G60"`） | 106ファイル中105ファイルに存在。欠落は `google405cf263b513c7a0.html` のみ | ✅ 問題なし（当該ファイルはSearch Console所有権確認用の空スタブで、タグ不要） |
| 本番サイトでの `gtag.js` 読み込み | `https://www.googletagmanager.com/gtag/js?id=G-CCCN9E7G60` が HTTP 200 で取得 | ✅ 正常 |
| `config` コマンドの発火 | `dataLayer` に `['config','G-CCCN9E7G60']` を確認。`window.gtag` 関数も存在 | ✅ 正常 |
| 計測ビーコン（`/g/collect`）の送信 | 本番ページ読み込み時にリソースとして送信されていることを確認 | ✅ 正常 |
| **GA4リアルタイムレポートへの反映** | 本番サイト閲覧後、「過去5分間のアクティブユーザー数」が 0 → **1** に増加 | ✅ **エンドツーエンドで到達** |

### 結論

**GA4タグは本番環境で正しく発火し、データがGA4に到達している。** したがって 8/20 以降の13日連続ゼロは計測事故ではなく、**実際に外部からの訪問が1件も発生していない**ことを意味する。

これは悪いニュースだが、少なくとも「数字が信用できない」状態は脱した。以後は数値をそのまま実態として読んでよい。

### 積み残しの論点

タグが正常である以上、**7月〜8月上旬にあった Organic Search / Referral の流入が8月中旬に消えた**理由は計測側では説明がつかない。サイト側・検索側の要因（インデックス状況、参照元リンクの消失）を疑う段階に移る。→ 次項へ。

---

## 数値から言えること・改善案

1. **計測は正常と確定したので、次の一手は「記事がインデックスされているか」の確認一択。** 8月を通じて `article-N.html` は67本すべて0PVで、閲覧されたのは `/`（29PV）と `/media/media.html`（3PV）のみ。記事一覧には到達しているのに記事本体に1PVも流れていないのは、(a) 検索結果に出ていない（未インデックス）、(b) 一覧ページのカードのリンクが機能していない、のどちらか。**まずSearch Consoleでインデックス済み件数を確認し、次に一覧ページのリンクを実際にクリックして確かめる。** この2つはどちらも所要10分程度で、他の施策より優先度が高い。

2. **記事は67本に増えているが、増やすほど成果から遠ざかっている可能性がある。** CLAUDE.mdの記載は53本だが実際は67本まで増えた。しかし8月の記事PVは0。**インデックスされていない状態で本数だけ増やしても数値は動かない。** 新記事の追加を一旦止め、既存67本をインデックスさせる作業（サイトマップ再送信、Search Consoleでの個別URL検査・インデックス登録リクエスト）に時間を振り向けるほうが費用対効果が高い。

3. **キーイベント（問い合わせフォーム送信）は8月も全期間0。** ただしセッション自体がほぼゼロなので、設定不備かどうかは依然として切り分けられない。計測の健全性が確認できた今、フォーム送信イベントが設定されているかをGA4の管理画面（イベント／キーイベント一覧）で直接確認するのが確実。流入が戻ったときに成果を取りこぼさないよう、先に整えておきたい。

## データ品質メモ

- 08-30 / 08-31 / 09-01 はいずれもGA確定処理後の取得。全て0のため後日ズレる可能性は低い。
- **09-02 のデータには、本レポートの検証作業で発生したアクセスが1〜2セッション含まれる**（本番トップページと `/media/media.html` を閲覧。参照元は `tagassistant.google.com` または direct）。翌日のレポート作成時、これを外部流入と誤読しないこと。
- 8/30・8/31を「未取得」としていた W35週次サマリー・8月月次サマリーは、本日の取得結果（いずれも0）を反映して更新済み。
